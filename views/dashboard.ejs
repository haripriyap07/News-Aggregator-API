<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title><%= title || 'Dashboard' %></title>
    <link rel="stylesheet" href="/css/style.css">
</head>
<body>
    <nav class="navbar">
        <div class="navbar-container">
            <a href="/" class="navbar-logo">📰 News Aggregator</a>
            <ul class="nav-menu">
                <li><a href="/feed">Feed</a></li>
                <li><a href="/saved">Saved</a></li>
                <li><a href="/preferences">Preferences</a></li>
                <li><a href="/digest">Digest</a></li>
                <li><a href="/dashboard">Dashboard</a></li>
                <li><a href="/logout" class="btn-logout">Logout</a></li>
                <li class="user-info">Welcome, <%= user.username %></li>
            </ul>
        </div>
    </nav>

    <div class="container">
        <h1>Your Dashboard</h1>

<div class="dashboard-grid">
    <div class="dashboard-card">
        <h2>📊 Statistics</h2>
        <div class="stat-item">
            <span class="stat-label">Saved Articles:</span>
            <span class="stat-value"><%= stats.savedArticles || 0 %></span>
        </div>
        <div class="stat-item">
            <span class="stat-label">Comments Made:</span>
            <span class="stat-value"><%= stats.comments || 0 %></span>
        </div>
        <div class="stat-item">
            <span class="stat-label">Interests Tracked:</span>
            <span class="stat-value"><%= stats.interests || 0 %></span>
        </div>
    </div>

    <div class="dashboard-card">
        <h2>⚙️ Your Preferences</h2>
        <div class="preferences-summary">
            <% if (preferences && preferences.interests && preferences.interests.length > 0) { %>
                <div class="summary-item">
                    <strong>Interests:</strong>
                    <p><%= preferences.interests.join(', ') %></p>
                </div>
            <% } else { %>
                <p class="no-data">No interests set. <a href="/preferences">Update preferences</a></p>
            <% } %>

            <% if (preferences && preferences.preferredSources && preferences.preferredSources.length > 0) { %>
                <div class="summary-item">
                    <strong>Preferred Sources:</strong>
                    <p><%= preferences.preferredSources.join(', ') %></p>
                </div>
            <% } %>

            <% if (preferences && preferences.keywordFilters && preferences.keywordFilters.length > 0) { %>
                <div class="summary-item">
                    <strong>Keyword Filters:</strong>
                    <p><%= preferences.keywordFilters.join(', ') %></p>
                </div>
            <% } %>
        </div>
    </div>

    <div class="dashboard-card">
        <h2>👤 Account Information</h2>
        <div class="account-info">
            <div class="info-item">
                <strong>Username:</strong>
                <p><%= user.username %></p>
            </div>
            <div class="info-item">
                <strong>Email:</strong>
                <p><%= user.email %></p>
            </div>
            <div class="info-item">
                <strong>Member Since:</strong>
                <p><%= new Date(user.createdAt).toLocaleDateString() %></p>
            </div>
        </div>
    </div>

    <div class="dashboard-card">
        <h2>🔗 Quick Links</h2>
        <div class="quick-links">
            <a href="/feed" class="dashboard-link">📰 Browse Feed</a>
            <a href="/saved" class="dashboard-link">💾 View Saved</a>
            <a href="/preferences" class="dashboard-link">⚙️ Edit Preferences</a>
            <a href="/digest" class="dashboard-link">📧 Daily Digest</a>
        </div>
    </div>
</div>

<style>
    .dashboard-grid {
        display: grid;
        grid-template-columns: repeat(auto-fit, minmax(300px, 1fr));
        gap: 2rem;
        margin-top: 2rem;
    }

    .dashboard-card {
        background: white;
        padding: 2rem;
        border-radius: 8px;
        box-shadow: 0 2px 8px rgba(0, 0, 0, 0.1);
    }

    .dashboard-card h2 {
        margin-top: 0;
        margin-bottom: 1.5rem;
        color: #333;
        border-bottom: 2px solid #007bff;
        padding-bottom: 1rem;
    }

    .stat-item {
        display: flex;
        justify-content: space-between;
        align-items: center;
        padding: 0.75rem 0;
        border-bottom: 1px solid #eee;
    }

    .stat-item:last-child {
        border-bottom: none;
    }

    .stat-label {
        color: #666;
        font-weight: 500;
    }

    .stat-value {
        font-size: 1.5rem;
        font-weight: bold;
        color: #007bff;
    }

    .preferences-summary {
        display: flex;
        flex-direction: column;
        gap: 1rem;
    }

    .summary-item {
        padding: 1rem;
        background: #f9f9f9;
        border-radius: 4px;
    }

    .summary-item strong {
        display: block;
        margin-bottom: 0.5rem;
        color: #333;
    }

    .summary-item p {
        margin: 0;
        color: #666;
        font-size: 0.95rem;
    }

    .no-data {
        color: #999;
        font-style: italic;
    }

    .no-data a {
        color: #007bff;
    }

    .account-info {
        display: flex;
        flex-direction: column;
        gap: 1rem;
    }

    .info-item {
        padding: 0.75rem;
        background: #f9f9f9;
        border-radius: 4px;
    }

    .info-item strong {
        display: block;
        color: #333;
        margin-bottom: 0.25rem;
    }

    .info-item p {
        margin: 0;
        color: #666;
    }

    .quick-links {
        display: flex;
        flex-direction: column;
        gap: 0.5rem;
    }

    .dashboard-link {
        display: block;
        padding: 1rem;
        background: #007bff;
        color: white;
        text-decoration: none;
        border-radius: 4px;
        text-align: center;
        transition: background 0.3s ease;
    }

    .dashboard-link:hover {
        background: #0056b3;
    }
</style>
    </div>

    <footer class="footer">
        <p>&copy; 2025 News Aggregator. All rights reserved.</p>
    </footer>

    <script src="/js/main.js"></script>
</body>
</html>
